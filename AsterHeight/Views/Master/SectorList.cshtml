@model AsterHeight.Models.Master

@{
    ViewBag.Title = "SectorList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm("SectorList", "Master", FormMethod.Post, new { @id = "form1" }))
{



    <section class="section">
        <ul class="breadcrumb breadcrumb-style ">
            <li class="breadcrumb-item">
                <a href="#">
                    <i data-feather="home"></i>
                </a>
            </li>
            <li class="breadcrumb-item">Master</li>
            <li class="breadcrumb-item">Sector List</li>
        </ul>
    </section>


    <div class="section-body">

        <div class="card">
            <div class="card-header">
                <h4>Sector List</h4>

                <p style="color: red">
                    @TempData["SectorMaster"]
                </p>
            </div>

            <div class="card-body">
                <div class="row">
                    <div class="col-md-12">
                        <!-- HTML5 Export Buttons table start -->
               
                            <div class="card-header table-card-header">

                                <a class="btn btn-primary btn-sm" style="margin-left:76%;margin-top:5px;" href="/Master/SectorMaster">Add New</a>
                            </div>
                            <div class="card-block">
                                <div class="table-responsive">
                                    <table class="table table-bordered table-md" style="width:100%;">
                                        <thead>
                                            <tr>
                                                <th>S No.</th>
                                                <th>SiteName</th>
                                                <th>Sector Name</th>

                                                <th></th>
                                            </tr>

                                        </thead>
                                        <tbody>
                                            @if (Model != null)
                                            {
                                                if (Model.lstSector != null)
                                                {
                                                    int i = 1;
                                                    foreach (var Item in Model.lstSector)
                                                    {
                                                        <tr class="text-left">
                                                            <td>@i</td>
                                                            <td>@Item.SiteName</td>
                                                            <td>@Item.SectorName</td>

                                                            <td>
                                                                <a href="/Master/SectorMaster?SectorID=@Item.EncryptKey" class="btn btn-info btn-sm">Edit</a>
                                                                <a href="/Master/DeleteSector?id=@Item.SectorID" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you wish to delete this sector?');">Delete</a>
                                                            </td>

                                                        </tr>

                                                        i++;
                                                    }
                                                }
                                            }

                                        </tbody>

                                    </table>
                                </div>
                            </div>
                        </div>
               
                </div>

            </div>
        </div>
    </div>

}



